[project]
name = "ray-revamped"
version = "0.1.0"
description = "Voice conversation robot pipeline"
requires-python = ">=3.11"
dependencies = [
    "einops>=0.8",
    "google-cloud-speech>=2.0,<3.0",
    "maai>=0.1",
    "omegaconf>=2.3",
    "openai>=1.66,<2.0",
    "onnxruntime>=1.24",
    "psutil>=7.2.2",
    "pyaudio>=0.2.14",
    "pytorch-lightning>=2.0",
    "rpi5-ws2812>=0.1.2",
    "sentence-transformers>=3.0,<4.0",
    "silero-vad>=5.0",
    "soundfile>=0.13",
    "tiktoken>=0.5",
    "torchmetrics>=1.8",
    "turngpt",
    "vap",
    "wandb>=0.25",
    "websockets>=14.0,<17.0",
]

[tool.uv.sources]
turngpt = { path = "external/TurnGPT", editable = true }
vap = { path = "external/VoiceActivityProjection", editable = true }

[project.optional-dependencies]
onnx-similarity = ["optimum[onnxruntime]"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.9",
]

[project.scripts]
ray = "voice_pipeline.__main__:main"

[tool.pytest.ini_options]
testpaths = ["voice_pipeline/tests"]
addopts = "-m 'not requires_model and not requires_api'"
markers = [
    "requires_model: tests requiring real model files",
    "requires_api: tests requiring real API credentials (Google Cloud, OpenAI, etc.)",
]

[tool.ruff]
target-version = "py311"
line-length = 99

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "SIM",  # flake8-simplify
]

[tool.ruff.lint.isort]
known-first-party = ["voice_pipeline"]
