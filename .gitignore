# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/


# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code — track shared settings and hooks, ignore local/runtime files
.claude/*
!.claude/settings.json
!.claude/hooks/

# OS
.DS_Store
Thumbs.db

# 기기별 설정 + API 키 (운영자가 손으로 쓰는 파일). 키 이름은 .env.example 에
.env
.env.*
!.env.example

# pytest
.pytest_cache/

# ruff
.ruff_cache/

# Models (downloaded via models.zip)
models/

# assets/ 는 전부 추적. 예외는 대용량 음원만 (별도 배포)
assets/audio/music/
assets/audio/vocal/

# 프로그램이 실행 중에 쓰는 파일 전부 (DB, 로그, 캐시, 생성 오디오, 평가 산출물)
var/

# External model repositories
external/

# C++ third-party libraries
third_party/


/CANDOR

# Temporary: research materials (large, not ready for tracking)
docs/memory-research/

# Large media / runtime logs / vendored libs / received bundles (distributed separately)
/*.wav
pmic_log_*.csv
OS_LED_setup.zip
WiringPi/
cpp/*.bak_*
*.log
/LED_handoff_bundle*/

# GPT-Live 실험 산출물
scripts/gpt_live/logs/
scripts/gpt_live/tts_cache/
scripts/gpt_live/*.wav
